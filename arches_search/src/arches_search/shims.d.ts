// Shim for packages whose @types declarations are not discoverable when
// arches-modular-reports source files are resolved from their installed
// Python package location (site-packages) rather than from node_modules.
declare module "numeral";
